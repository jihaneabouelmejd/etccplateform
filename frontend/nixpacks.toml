# ============================================================
# Railway NixPacks config — ETCC Frontend (Next.js)
# ============================================================

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
